# Prompt 02 — Content contract

```text
Hãy dùng $tongluc-wordpress-production. Dựa trên sitemap/IA đã duyệt và docs/page-wave-status.md khi đã có active wave, tạo content contract JIT chỉ cho [GLOBAL SHELL/HOME/ARCHETYPE/TRANG] từ docs/content-contracts/_template.md.

Khóa mục tiêu, intent, thông điệp, heading, CTA, evidence/claim cần xác minh, entity, media, dữ liệu động và acceptance criteria. Greenfield dùng Rank Math làm schema owner; existing dùng engine đã duyệt. Một contract có thể phục vụ nhiều instance cùng archetype; chỉ tách khi khác intent hoặc conversion path có ý nghĩa. Không tạo contract chi tiết cho mọi launch page trong cùng lượt trừ khi owner chọn batch-production rõ ràng.

Không chọn widget và chưa dựng giao diện. Cập nhật STATUS rồi dừng để duyệt contract.
```
