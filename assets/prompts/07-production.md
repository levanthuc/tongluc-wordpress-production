# Prompt 08 — Page Wave Production

```text
Hãy dùng $tongluc-wordpress-production để triển khai active scope trong docs/page-wave-status.md. Trước write, xác nhận Global Foundation, Global Shell và Home Prototype đang `Approved`; revision content contract/blueprint liên quan còn khớp và live target khớp build manifest hoặc là target mới.

Chỉ xử lý một active scope. Nếu preconfigured baseline `Current`, tái sử dụng exact page/layout/menu/form/Woo target và settings `Keep/Skip`; không inventory, tạo trùng hoặc reapply. Trang độc nhất build/QA rồi dừng để duyệt. Representative archetype phải duyệt trước khi tạo instance; chỉ batch khi delivery mode/scope cho phép. Dùng Elementor Free V3 + UAE Pro, widget native, Astra Customizer và Astra Site Builder đúng ownership. Form preset dùng Shortcode/UAE styler, không HTML khi widget phù hợp. Không dùng Atomic/V4, schema trùng, claim/evidence giả hoặc đổi LiteSpeed settings.

Trước first write của mỗi MCP ability trong run này, lấy live ability info; cache chỉ dùng để bỏ full discovery. Xác nhận target/rollback, write, read back rồi cập nhật build manifest; artifact managed từ phiên trước không phải profile exception. Kiểm tra responsive, heading, keyboard/focus/contrast/alt, link/query/form state và frontend đăng xuất. Purge theo policy. Cập nhật STATUS; báo URL/template thay đổi, test đạt và tồn đọng.
```
