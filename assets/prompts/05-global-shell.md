# Prompt 05 — Global Shell

```text
Hãy dùng $tongluc-wordpress-production. Chỉ bắt đầu khi IA/menu và Global Foundation đúng revision đều Approved.

Nếu preconfigured baseline `Current`, dùng page/menu/layout ID và visual rule đã ghi; không inventory, tạo trùng hoặc reconfigure mục `Keep/Skip`. Nếu chưa có target preset, tạo page shells/menu theo IA khi cần. Thiết kế header, footer và hook/pre-footer bằng Astra Builder/Site Builder đúng ownership. Không dùng Hook thay footer thật nếu Footer layout phù hợp hơn; không để header/footer trùng condition.

Trước first write của mỗi MCP ability, lấy live ability info; resolve target, write, read-back và cập nhật build manifest. Kiểm tra frontend đăng xuất desktop/tablet/mobile, sticky/transparent/off-canvas, keyboard/focus và display conditions. Ghi docs/global-shell-approval.md, cập nhật STATUS và dừng tại cổng duyệt Global Shell; chưa dựng Home.
```
