# Prompt 04 — Design foundation

```text
Hãy dùng $tongluc-wordpress-production. Đọc docs/STATUS.md và chỉ các file trong Current inputs; đối chiếu dependency đã duyệt với Approved outputs và marker nguồn.

Nếu STATUS ghi preconfigured baseline `Current`, đọc đúng revision trong Current inputs, tin mọi mục `Keep/Skip`, không audit/reapply/upload lại và chỉ thực hiện `Action required`/delta. Nếu không có, hoàn thiện rồi áp dụng docs/design-foundation.md: palette, typography, container, spacing, button, form, radius, image policy, logo và favicon. Astra Customizer là nguồn global; Elementor/UAE kế thừa theme.

Khi cần site write, dùng MCP JIT và live ability info theo SOP; resolve/read-back rồi cập nhật build manifest. Chưa dựng header/footer hoặc trang nội dung. Chỉ kiểm tra frontend phần vừa thay đổi và dependency trực tiếp; ghi bằng chứng, cập nhật STATUS rồi dừng tại cổng Global Foundation.
```
